let WIDTH = 40;
let title = "Flintstone Family Members";
let spacesOnEachSide = Math.floor((WIDTH - title.length) / 2);

console.log(" ".repeat(spacesOnEachSide) + title + " ".repeat(spacesOnEachSide));